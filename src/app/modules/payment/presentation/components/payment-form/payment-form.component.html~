<div class="bg-white p-6 shadow-lg rounded-lg">
    <div class="flex justify-between items-center mb-4">
        <h2 class="text-lg font-semibold">Ajouter un paiement</h2>
        <button (click)="handleClose()" class="text-gray-500 hover:text-gray-800">✖</button>
    </div>

    <label class="block text-sm font-medium text-gray-700">Date</label>
    <input type="text" class="w-full p-2 border rounded-lg bg-gray-100 mb-3" [value]="'13/05/2025'" readonly>

    <label class="block text-sm font-medium text-gray-700">Bénéficiaire</label>
    <input type="text" class="w-full p-2 border rounded-lg bg-gray-100 mb-3" placeholder="Nom du bénéficiaire">

    <label class="block text-sm font-medium text-gray-700">Montant (€)</label>
    <input type="number" class="w-full p-2 border rounded-lg bg-gray-100 mb-3" placeholder="0.00">

    <label class="block text-sm font-medium text-gray-700">Type</label>
    <select class="w-full p-2 border rounded-lg bg-gray-100 mb-3">
        <option>Facture</option>
        <option>Abonnement</option>
    </select>

    <label class="block text-sm font-medium text-gray-700">Statut</label>
    <select class="w-full p-2 border rounded-lg bg-gray-100 mb-3">
        <option>En attente</option>
        <option>Paiement effectué</option>
    </select>

    <button (click)="handleSave()" class="w-full bg-black text-white py-2 rounded-lg mt-3"><mat-icon svgIcon="feather:save"></mat-icon> Enregistrer le paiement</button>
</div>
